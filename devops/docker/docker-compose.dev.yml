# Development override — hot reload, volumes
version: "3.9"
services:
  backend:
    volumes:
      - ../../backend/app:/code/app
    environment:
      - DEBUG=true
  frontend:
    volumes:
      - ../../frontend/src:/app/src
    ports:
      - "5173:5173"
