/** MonitoringDashboard — Monitoring component. */
import Card from "../ui/Card";

export default function MonitoringDashboard(_props: Record<string, never>) {
  return (
    <Card>
      <h3 className="text-sm font-semibold text-gray-300 uppercase tracking-wider mb-4">
        MonitoringDashboard
      </h3>
      <p className="text-gray-400 text-sm">
        MonitoringDashboard component placeholder.
      </p>
    </Card>
  );
}
