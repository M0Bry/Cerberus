/** HealthStatusCard — Monitoring component. */
import Card from "../ui/Card";

export default function HealthStatusCard(_props: Record<string, never>) {
  return (
    <Card>
      <h3 className="text-sm font-semibold text-gray-300 uppercase tracking-wider mb-4">
        HealthStatusCard
      </h3>
      <p className="text-gray-400 text-sm">
        HealthStatusCard component placeholder.
      </p>
    </Card>
  );
}
