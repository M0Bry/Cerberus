/** BillingSubscriptions — Admin component. */
import Card from "../ui/Card";

export default function BillingSubscriptions(_props: Record<string, never>) {
  return (
    <Card>
      <h3 className="text-sm font-semibold text-gray-300 uppercase tracking-wider mb-4">
        BillingSubscriptions
      </h3>
      <p className="text-gray-400 text-sm">
        BillingSubscriptions component placeholder.
      </p>
    </Card>
  );
}
