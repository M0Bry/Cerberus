/** UserManagementTable — Admin component. */
import Card from "../ui/Card";

export default function UserManagementTable(_props: Record<string, never>) {
  return (
    <Card>
      <h3 className="text-sm font-semibold text-gray-300 uppercase tracking-wider mb-4">
        UserManagementTable
      </h3>
      <p className="text-gray-400 text-sm">
        UserManagementTable component placeholder.
      </p>
    </Card>
  );
}
