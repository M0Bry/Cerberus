/** RoleManager — Admin component. */
import Card from "../ui/Card";

export default function RoleManager(_props: Record<string, never>) {
  return (
    <Card>
      <h3 className="text-sm font-semibold text-gray-300 uppercase tracking-wider mb-4">
        RoleManager
      </h3>
      <p className="text-gray-400 text-sm">
        RoleManager component placeholder.
      </p>
    </Card>
  );
}
